# 羽毛球视频自动剪辑系统 - Python依赖包

# Web前端框架
Flask>=2.0.0
Werkzeug>=2.0.0

# 深度学习框架
torch>=1.8.0
torchvision>=0.9.0

# 计算机视觉
opencv-python>=4.5.0

# 数据处理
numpy>=1.19.0

# 配置文件解析
pyyaml>=5.4.0

# 进度条显示
tqdm>=4.60.0

# 可选：CUDA加速（如果使用GPU）
# torch==1.13.1+cu117
# torchvision==0.14.1+cu117

# 注意：
# 1. FFmpeg需要单独安装，不在pip依赖中
# 2. 如果使用GPU，请根据CUDA版本安装对应的PyTorch
# 3. 安装命令：pip install -r requirements.txt
